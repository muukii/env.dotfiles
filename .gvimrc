set guioptions=agirLt

set linespace=1
set columns=80
set lines=40
set cmdheight=2
colorscheme concise

if has('mac')
  set transparency=3
  set linespace=2
endif

if has('mac')
  set guifont="Roboto Mono"
endif
